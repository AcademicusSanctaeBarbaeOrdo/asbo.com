<div class="col-lg-12">

    <div class="panel panel-default">
        <div class="panel-heading"><i class="fa fa-comment"></i> Timeline</div>
        <div class="panel-body">
            {% for entry in timeline %}

                {{ timeline_render(entry) }}

                {% if not loop.last %}
                    <hr />
                {% endif %}

            {% else %}
                No action for now ...
            {% endfor %}
            </ul>
        </div>
    </div>
</div>
<hr />
